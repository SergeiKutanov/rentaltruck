{% extends '::admin.html.twig' %}
{% form_theme edit_form 'form_table_layout.html.twig' %}

{% block middle_bar -%}
    <h1>Organisation edit</h1>

    <form action="{{ path('organisation_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('organisation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('organisation_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
