{% extends '::admin.html.twig' %}
{% form_theme edit_form 'form_table_layout.html.twig' %}
{% block middle_bar %}
    <h1>Редактирование клиента</h1>

    <form action="{{ path('admin_client_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_client') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('admin_client_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
